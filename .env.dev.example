# Optional — only needed for: make test-integration
# Copy to .env.dev and source together with .env, or export manually.

# TEST_DB_HOST=localhost
# TEST_DB_PORT=5432
# TEST_DB_NAME=loggerdb_test
# TEST_DB_USER=postgres
# TEST_DB_PASSWORD=postgres
# TEST_DB_SSLMODE=disable

# Optional: interactive API docs at http://localhost:8080/swagger/
# ENABLE_SWAGGER_UI=true
